import { defineAocModule, readLines } from "@/lib";

const lines: string[] = readLines("day18/input.txt");

function sol1(): number {
  return 0;
}

function sol2(): number {
  return 0;
}

export default defineAocModule({
  day: 18,
  exp1: 0,
  exp2: 0,
  sol1,
  sol2,
});
